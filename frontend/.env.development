# Base path for API calls (same-origin via Vite proxy in dev, FastAPI in prod).
VITE_API_BASE=/api
# Backend target for the dev proxy (override if not on localhost:8765).
VITE_API_TARGET=http://localhost:8765
